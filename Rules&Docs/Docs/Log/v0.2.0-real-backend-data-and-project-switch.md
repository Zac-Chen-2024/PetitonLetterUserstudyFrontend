# v0.2.0 — Real backend data + project switcher restored

Date: 2026-04-22
Scope: frontend only
Status: implemented, verified via production build + dev HMR

---

## Task Understanding

- **Goal**: Stop serving the mocked userstudy demo. Route the three-column
  Write Mode workspace through the real `AppProviders` data pipeline so it
  loads project data from the backend, defaulting to Dehuan Liu, and surface
  the Header's existing project switcher so users can change projects at
  runtime.
- **Scope of change** (frontend only):
  1. Introduce `frontend/src/pages/main/MainPage.tsx` — the new production
     entry that renders Header + three columns backed by real data.
  2. Rewrite `frontend/src/App.tsx` to wrap `MainPage` in `AppProvider`
     (`AppProviders` re-export), without a `projectIdOverride`.
  3. Change `DEFAULT_PROJECT_ID` in `ProjectContext.tsx`
     from `'yaruo_qu'` → `'dehuan_liu'`.
  4. Delete `frontend/src/demo/` (old `DemoPage.tsx` + `mockData.ts`).
- **What was NOT changed**:
  - `Header.tsx`, `projectService.ts`, `AppProviders`/`DataLoader`,
    `ProjectContext` state machinery — the existing real-data pipeline is
    untouched. This is reuse, not refactor.
  - `frontend/src/services/api.ts` — `VITE_API_BASE` default
    (`https://plus.drziangchen.uk/api`) is kept. GH Pages build and local
    dev both hit the same production backend, which carries the Dehuan Liu
    project (verified via curl).
  - Routing — single `/` route, nothing else.
  - Backend — completely untouched.
  - `frontend/src/video/drHuVideoScenario.ts` and `videoDemoPresets.ts` — still
    transitively imported by shared components. Cleanup deferred.
- **Assumptions** (validated before coding):
  - Both `dr_hu_eb1a` and `dehuan_liu` exist on the backend (`/api/projects`
    confirmed returns both on localhost:8808 and plus.drziangchen.uk).
  - The "project switcher" the user wants is the dropdown already built into
    `Header.tsx` (list + search + select + create); no new UI needed.
  - Write mode is the intended initial view. `workMode` defaults to
    `'verify'` in `UIContext`; `MainPage` pushes it to `'write'` on mount.

## Implementation Plan

1. Create `frontend/src/pages/main/MainPage.tsx` (Header + three-column
   layout using real `DocumentViewer compact`, `EvidenceCardPool`,
   `ArgumentGraph`, `LetterPanel`, `ConnectionLines`; small `InitWorkMode`
   effect component to call `setWorkMode('write')` once).
2. Rewrite `App.tsx` to import `MainPage` and wrap in `AppProvider` without
   a `projectIdOverride`.
3. Change `DEFAULT_PROJECT_ID` to `'dehuan_liu'`.
4. `rm -rf frontend/src/demo/`.
5. `npm run build` to confirm type-strippable production build still works.
6. Verify dev server HMR accepts the changes.
7. Write this log, commit, push.

## Changes Made

### Files added
- `frontend/src/pages/main/MainPage.tsx`

### Files modified
- `frontend/src/App.tsx` — import `MainPage` from `./pages/main/MainPage`;
  wrap in `AppProvider` (no override).
- `frontend/src/context/ProjectContext.tsx` — `DEFAULT_PROJECT_ID` →
  `'dehuan_liu'`.

### Files deleted
- `frontend/src/demo/DemoPage.tsx`
- `frontend/src/demo/mockData.ts`
- `frontend/src/demo/` directory (now empty, removed)

## Verification

- `npm run build` → Vite 7.3.2 production build succeeded, 269 modules
  transformed (down from 270; `mockData.ts` removed), 7.16s.
- `npm run dev` (still running) — HMR accepted App.tsx and ProjectContext
  edits without error; fetched `/src/pages/main/MainPage.tsx` → 200.
- `curl http://localhost:8808/api/projects` → returns `dehuan_liu` and
  `dr_hu_eb1a`.
- `curl https://plus.drziangchen.uk/api/projects` → same, confirming the
  production backend used by the frontend (default `VITE_API_BASE`) has
  the Dehuan Liu data.

## Risks / Pending

- **Browser runtime not exercised**: A real user session should confirm:
  (a) the default load lands on Dehuan Liu, (b) the Header dropdown opens
  and switches projects, (c) EvidenceCardPool, DocumentViewer, ArgumentGraph,
  LetterPanel render against the real data without console errors. I only
  verified module compilation and backend availability.
- **localStorage carryover**: `ProjectContext` persists the last chosen
  projectId to `evidence-system-project-id`. Users who previously pinned
  `dr_hu_eb1a` (via the archived `projectIdOverride="dr_hu_eb1a"` shell)
  will land on Dr. Hu on first visit — they can switch via the Header
  dropdown. Clearing site storage in devtools forces the new default.
- **Dehuan Liu pipeline stage**: `metadata.json` reports `ocr_complete` with
  extraction present. `arguments/` and `writing/` on disk contain some data
  but the ArgumentGraph and LetterPanel may render partially populated —
  this is a data-state outcome, not a UI bug. `dr_hu_eb1a` is fully
  populated for end-to-end testing.
- **Bundle size warning**: `dist/assets/index-*.js` is ~906 kB (~270 kB
  gzipped). Vite flags this as large; code-splitting is a future
  optimization, not in scope here.
- **Rollback**: `git revert` the upcoming commit. Mock mode components and
  `mockData.ts` remain recoverable from v0.1.0 git history or
  `archive.tar.gz`.
